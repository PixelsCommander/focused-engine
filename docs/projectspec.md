Implement a webgl r3f 3d scene showing jet turbine from front.


Viewport

HTML container fills the whole screen but the whole page is longer so we can scroll down. Turbine front should fill the whole viewport. There
should be some monochromatic environment map. 

Mouse interaction

Point light to follow the mouse pointer. Enginer slightly turns
following the mouse. 

Animation
Engine propeller spins with a speed based on how much screen is scrolled. So if it is 50% scrolled through it spins 2x faster. 

Model
Use model from /assets/jen_engine.glb